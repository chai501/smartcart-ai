# Copy to .env.local and fill in your values.
# Vite only exposes vars prefixed with VITE_ to the browser.

# The deployed backend URL (Render free service URL).
# Leave blank to use the Vite dev-proxy (localhost:5000) during development.
VITE_API_URL=
